{
  "name": "math-ai",
  "version": "0.2.0",
  "private": true,
  "description": "AI 数学学习与出题系统（前后端一键启动）",
  "scripts": {
    "install:all": "cd server && npm install && cd ../web && npm install",
    "dev:server": "cd server && npm run dev",
    "dev:web": "cd web && npm run dev",
    "dev": "concurrently -n 后端,前端 -c blue,green \"npm run dev:server\" \"npm run dev:web\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
